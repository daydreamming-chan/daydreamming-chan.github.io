# 展示页图片放置指南

本文件说明 `_showcase/` 下每个卡片添加图片时，图片应放置的目录与引用方式。

## 图片目录结构

```
assets/images/
├── badges/      # 院校 / 机构徽标
├── covers/      # 封面图、横幅图
├── etc/         # 通用图片（示例、照片等）
├── photos/      # 个人头像、生活照（首页 profile_card 已引用 portrait.jpg）
└── empty_300x200.png  # 懒加载占位图（请勿删除）
```

> 所有图片路径在 Markdown 中通过 `relative_url` 过滤器引用，示例：
> `{{ 'assets/images/etc/your.jpg' | relative_url }}`

---

## 默认分组（`_showcase/default/`）

| 卡片文件 | 图片目录 | 引用示例 |
|---|---|---|
| `badge1.md` | `assets/images/badges/` | `<img src="{{ 'assets/images/badges/xxx.png' | relative_url }}" class="img-fluid rounded-xl">` |
| `badge2.md` | `assets/images/badges/` | 同上 |
| `badge3.md` | `assets/images/etc/` | `<img src="{{ 'assets/images/etc/xxx.png' | relative_url }}" class="img-fluid rounded-xl">` |
| `image_text_1.md` | `assets/images/covers/` | `<img data-src="{{ 'assets/images/covers/xxx.jpg' | relative_url }}" class="lazy w-100 rounded-xl" src="{{ '/assets/images/empty_300x200.png' | relative_url }}">` |
| `image_text_2.md` | `assets/images/covers/` | 同上（建议 `rounded-xl-top`） |
| `photo_collection.md` | `assets/images/`（自定义子目录均可） | 在 front matter 的 `images` 列表中填写 `src: /assets/images/xxx/xxx.jpg` |
| `welcome.md` | 无图片 | — |
| `how_to_disable.md` | 无图片 | — |
| `education.md` | 无图片（从 `profile.yml.education[*].logo` 读取） | 在 `_data/profile.yml` 的 education 项里配置 `logo: /assets/images/badges/xxx.png` |
| `formula.md` | 无图片（公式渲染） | — |

---

## Cats 分组（`_showcase/cats/`）

| 卡片文件 | 图片目录 | 引用示例 |
|---|---|---|
| `cat.md` | `assets/images/etc/` | `<img src="{{ 'assets/images/etc/xxx.jpg' | relative_url }}" class="img-fluid rounded-xl">` |
| `cat2.md` | `assets/images/etc/` | `<img data-src="{{ 'assets/images/etc/xxx.jpg' | relative_url }}" class="lazy w-100 rounded-xl" src="{{ '/assets/images/empty_300x200.png' | relative_url }}">` |
| `cat3.md` | `assets/images/etc/`（原为远程图，建议改本地） | 同 `cat2.md` |
| `cat4.md` | `assets/images/etc/`（原为远程图，建议改本地） | 同 `cat2.md` |

---

## 添加图片的通用步骤

1. 将图片文件放入上表对应的目录（例如 `assets/images/etc/my_photo.jpg`）
2. 打开对应的 `.md` 卡片文件
3. 将占位 `<div class="...图片占位...">` 整段替换为 `<img>` 标签
4. 懒加载图片请保留 `data-src` 与占位 `src="{{ '/assets/images/empty_300x200.png' | relative_url }}"`
5. 保存后 Jekyll 会自动重新生成，刷新展示页即可

## 新建卡片的命名规则

- 文件名格式：`任意名称.md`，置于 `_showcase/default/` 或 `_showcase/cats/`
- front matter 必填字段：`show: true`、`width: 1~12`、`date: YYYY-MM-DD HH:MM:SS +0800`
- 可选字段：`group: 分组名`（不填则归入默认分组）、`class: ""`（覆盖默认卡片样式）
